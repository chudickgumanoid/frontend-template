/**
 * Naive UI theme configuration.
 * Define overrides here and pass them through the app provider
 * (e.g. `NConfigProvider`) via `theme`, `themeOverrides`, `locale`, `dateLocale`.
 */
export const themeConfig = {};
