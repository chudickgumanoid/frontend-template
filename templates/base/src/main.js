/**
 * App entry point.
 *
 * - Imports the pre-configured Vue application instance from `./app`.
 * - Mounts it to the DOM element with id `app`.
 */
import { application } from "./app";

application.mount("#app");
